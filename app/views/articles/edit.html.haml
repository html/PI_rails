- title "Edit Article"

= render :partial => 'form', :locals => { :url => { :controller => :articles, :action => :update, :id => @article.to_param  } }

%p
  = link_to "Show", article_path(@article)
  |
  = link_to "View All", articles_path
